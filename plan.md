# dango

## Now (this PR)

- Live `gh` via `--repo owner/name`. Cwd `git remote` can come later.
- `--provider` optional. Summaries async after first paint. Never block the first frame.
- Two-col list: name + balls. Title swap in-place. Description in the inspector only.
- Snap leva-2 when fetch works: `--repo gsimone/leva-2`.

## Next PR (snaps to Bobert — do not wait on Gianmarco)

- Full-screen provider/model picker (list, enter, esc). `--provider` still overrides.
- `dango` in a repo reads `git remote` + `dango.json`. `--repo` wins.
- Missing `dango.json` = no generated title.

## Locked on this PR

- No status column.
- Packed `●-●-●` logo.
- Status inks on the balls.
- No settings screen. No picker.

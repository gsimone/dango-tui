# dango

## Now (this PR)

- Live `gh` via `--repo owner/name`.
- Two-col list: name + balls.
- No picker. No settings screen.

## Next PR (after fetch)

- `dango.json` + cwd `git remote` detection.
- `--repo` still wins.
- No settings screen in that PR.

## Later (do not ship until asked)

- Full-screen provider list (enter / esc).
- `--provider` still overrides.

## Locked

- No status column.
- Packed `●-●-●` logo.
- Status inks on the balls.
